import { Container } from "@mui/material";
import { Game } from "../components/Game";

const MainGameBoard = () => {
  return (
    <Container>
      <Game />
    </Container>
  );
};

export default MainGameBoard;
